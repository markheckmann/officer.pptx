# officer.pptx 0.1.0

* `example_file` and `test_file` to find example and test files.
* added several xml and pptx helper functions incl. tests
* `pptx_text_replace`: replace text patterns in powerpoint slides. Formatting is respected.
