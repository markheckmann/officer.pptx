% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/officer-patches.R
\name{img}
\alias{img}
\title{Replacement for \code{external_image}}
\usage{
img(
  src,
  fit = "inside",
  scale = 1,
  h_just = 0.5,
  v_just = 0.5,
  x_offset = 0,
  y_offset = 0,
  offset_mode = "source",
  rotation = 0,
  background = "transparent",
  line = sp_line(),
  alt = "",
  unit = "in",
  .left = NULL,
  .top = NULL,
  .width = NULL,
  .height = NULL
)
}
\arguments{
\item{src}{Image file path.}

\item{fit}{\verb{[character]}\cr One of \code{inside}: fit inside target frame, \code{outside}: fit around target frame,
\code{width}: match target frame width, \code{height}: match target frame height, \code{none}: no scaling.
If \code{FALSE}, scale frame, so it contains the target frame.}

\item{scale}{\verb{[numeric >= 0]}\cr Value to scale image by.}

\item{h_just, v_just}{\verb{[numeric]}\cr Horizontal / vertical placement of image inside frame.
\verb{0=}left/bottom, \verb{1=}right/top. By default, the image is centered.}

\item{x_offset, y_offset}{\verb{[numeric]}\cr Offset as multiple of scaled source frame width / height (default) or raw
units (see \code{offset_mode}).}

\item{offset_mode}{\verb{[character]}\cr Offset values are interprete as one of: multiple of scaled source frame's
width / height (\code{source}, default), multiple of target frame's width / height (\code{target}), or raw \code{units}.}

\item{rotation}{Rotation in degrees (anti-clockwise).}

\item{background}{Background color (hex or color name).}

\item{line}{Border around image. Either a color or an \link[officer:sp_line]{officer::sp_line} object for more control.}

\item{alt}{Alternative text for image.}

\item{unit}{One of \code{"in"} (default), \code{"cm"}, \code{"mm"}. Applies to offsets if \code{offset_mode = "units"}, and \code{.left},
\code{.top}, \code{.width}, and\code{.height}.}

\item{.left, .top, .width, .height}{Overwrite placeholder coordinates manually. \code{unit}s apply.}
}
\description{
Replacement for \code{external_image}
}
\examples{
file_pptx <- tempfile(fileext = ".pptx")
file_png <- tempfile(fileext = ".png")

# sample image
png(file_png, bg = "transparent")
  plot(1:10, col = "red", pch = 16, cex = 3)
  title("PNG")
dev.off()

x <- read_pptx() |> layout_default("Title and Content")

# default settings
x <- x |> add_slide(title = "img() - defaults", body = img(file_png))

# img() settings
x <- x |> add_slide(
  title = "img() - with settings",
  body = img(file_png, h_just = 0, line = "blue", background = "lightblue")
)

# settings via ph_with()
x <- x |>
  add_slide(title = "img() - with settings") |>
  ph_with(img(file_png), "body",
    line = sp_line(lwd = 4, color = "red", lty = "dash"),
    background = "#ffdddd", rotation = 90
  )

# via phs_with() ---
x <- x |>
  add_slide() |>
  phs_with(
    fullsize = img(file_png, scale = .5, v_just = 1, h_just = 0),
    fullsize = img(file_png, scale = .5, v_just = 0, h_just = 0),
    right = img(file_png)
  )

# More img() options
x <- x |> add_slide(body = img(file_png, fit = "inside", scale = .65, rotation = 45))

# possible to add mutiple times at same ph position
x <- x |> add_slide() |> phs_with(
  body = img(file_png, fit = "inside", scale = .5, x_offset = -1),
  body = img(file_png, fit = "inside", scale = .5),
  body = img(file_png, fit = "inside", scale = .5, x_offset = 1)
)

print(x, file_pptx)
\dontrun{
file_open(file_pptx)} # may not work on all systems
}
