% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ph_visualize.R
\name{ph_visualize}
\alias{ph_visualize}
\title{Visualize placeholders on a slide}
\usage{
ph_visualize(
  x,
  ph = NULL,
  slide_idx = NULL,
  fill = NULL,
  line = sp_line(color = "#1565C0", lwd = 2, lty = "dash"),
  label = FALSE
)
}
\arguments{
\item{x}{\verb{[rpptx]}\cr An rpptx object. See \code{\link[officer:read_pptx]{officer::read_pptx()}}.}

\item{ph}{Placeholder specification. Can be:
\itemize{
\item \code{NULL} (default): all placeholders on the slide
\item Type string: \code{"body"}, \code{"title"}, \code{"ftr"}, \code{"dt"}, \code{"sldNum"}, etc.
\item Type with index: \code{"body[1]"}, \code{"body[2]"}
\item Label string: \code{"Content Placeholder 2"}
\item Special: \code{"left"}, \code{"right"}
\item A \verb{ph_location_*()} object: \code{\link[officer:ph_location_type]{officer::ph_location_type()}},
\code{\link[officer:ph_location_label]{officer::ph_location_label()}}, \code{\link[officer:ph_location_id]{officer::ph_location_id()}}
\item Character vector for multiple: \code{c("body", "title")}
}}

\item{slide_idx}{\verb{[integer]}\cr Slide index. \code{NULL} (default) uses the current slide.}

\item{fill}{Fill color (e.g., \code{"#BBDEFB"}, \code{"lightblue"}). Default \code{NULL} (no fill).}

\item{line}{Border specification via \code{\link[officer:sp_line]{officer::sp_line()}}. Default is a dashed
blue line. Use \code{NULL} for no border.}

\item{label}{\verb{[logical]}\cr Show placeholder label/type as text overlay?
Default \code{FALSE}.}
}
\value{
The rpptx object with visualization overlays added.
}
\description{
Adds visual indicators (border and/or fill) to placeholders on the current
slide to help understand the layout. Useful for debugging placeholder
positions and designing slide content.
}
\examples{
library(officer)

# All placeholders with default dashed border
read_pptx() |>
  add_slide(layout = "Two Content") |>
  ph_visualize()

# Specific placeholder with fill
read_pptx() |>
  add_slide(layout = "Title and Content") |>
  ph_visualize(ph = "body", fill = "#FFE4E1")

# Multiple placeholders with labels
read_pptx() |>
  add_slide(layout = "Two Content") |>
  ph_visualize(ph = c("body[1]", "body[2]"), label = TRUE)

# Using ph_location_type()
read_pptx() |>
  add_slide(layout = "Title and Content") |>
  ph_visualize(ph = ph_location_type("body"), fill = "lightblue")

}
