% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-update.R
\name{shape_update}
\alias{shape_update}
\title{Update selected slide shapes}
\usage{
shape_update(
  x,
  selection,
  text = NULL,
  background = NULL,
  line = NULL,
  left = NULL,
  top = NULL,
  width = NULL,
  height = NULL,
  rotation = NULL,
  name = NULL,
  description = NULL,
  hidden = NULL,
  empty = c("error", "ignore")
)
}
\arguments{
\item{x}{\verb{[rpptx]}\cr An \code{rpptx} object returned by \code{\link[officer:read_pptx]{officer::read_pptx()}}.}

\item{selection}{\verb{[pptx_shape_selection]}\cr A selection returned by
\code{\link[=shape_select]{shape_select()}} for \code{x}.}

\item{text}{\verb{[character]}\cr Replacement text. If supplied, the complete text
of each selected text-capable shape is replaced. The first existing text run
keeps its formatting.}

\item{background}{\verb{[character]}\cr Solid background color. Use
\code{"transparent"} to force no fill.}

\item{line}{\verb{[sp_line]}\cr An \code{\link[officer:sp_line]{officer::sp_line()}} object, or a list of
\code{sp_line} objects, used as outline style.}

\item{left, top, width, height}{\verb{[numeric]}\cr Shape position and size in inches.
\code{NULL} leaves the current value unchanged.}

\item{rotation}{\verb{[numeric]}\cr Shape rotation in degrees. Positive values use
the same convention as \code{\link[officer:ph_location]{officer::ph_location()}}.}

\item{name}{\verb{[character]}\cr Shape name as shown in PowerPoint's Selection Pane.}

\item{description}{\verb{[character]}\cr Alternative text description. Use \code{NA} to
remove an existing description.}

\item{hidden}{\verb{[logical]}\cr Whether to hide the shape.}

\item{empty}{\verb{[character(1)]}\cr What to do with an empty selection. The
default, \code{"error"}, fails early. Use \code{"ignore"} to return \code{x} unchanged.}
}
\value{
The modified \code{rpptx} object.
}
\description{
\code{shape_update()} modifies existing top-level shapes returned by
\code{\link[=shape_select]{shape_select()}}. It edits the selected XML nodes in place and returns the
modified \code{rpptx} object.
}
\details{
Selection is based on existing slide objects, not PowerPoint layout
placeholders. Ordinary non-placeholder shapes are supported and are the main
use case.
}
\examples{
x <- read_pptx()
x <- add_slide(x, layout = "Blank")
x <- ph_with(
  x,
  "{status}",
  location = ph_location(
    left = 1, top = 1, width = 2.5, height = 1,
    newlabel = "Status marker"
  )
)

targets <- shape_select(x, text = "{status}", match = "exact")

x <- shape_update(
  x,
  targets,
  text = "Delayed",
  background = "#FCE8E6",
  line = sp_line(color = "#EC0016", lwd = 1.5),
  description = "Current service status"
)

shape_select(x, name = "Status marker", match = "exact")
}
