% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_replace.R
\name{text_replace}
\alias{text_replace}
\title{Replace text on slides}
\usage{
text_replace(
  x,
  pattern = NULL,
  replacement = NULL,
  slide_idx = NULL,
  ...,
  ph_type = NULL,
  exclude_ph_type = NULL,
  verbose = 1L,
  dry_run = FALSE
)
}
\arguments{
\item{x}{\verb{[rpptx]}\cr An \code{rpptx} object returned by \code{\link[officer:read_pptx]{officer::read_pptx()}}.}

\item{pattern}{\verb{[character]}\cr Vector with patterns to replace. Regex is not interpreted.}

\item{replacement}{\verb{[character]}\cr Vector with replacements.}

\item{slide_idx}{\verb{[numeric]}\cr Index of slides to process. If \code{NULL} (default), all slides
are processed.}

\item{...}{\verb{[key=value]}\cr Comma separated \code{'pattern'='replacement'} pairs as an alternative to
using the \code{pattern} and \code{replacement} args.}

\item{ph_type}{\verb{[character]}\cr Placeholder types to include (e.g., \code{"body"}, \code{"title"},
\code{"ftr"}, \code{"dt"}, \code{"sldNum"}). If \code{NULL} (default), all shapes are included.
Non-placeholder shapes are excluded when this is set.}

\item{exclude_ph_type}{\verb{[character]}\cr Placeholder types to exclude. Applied after
\code{ph_type} filtering. Non-placeholder shapes are not affected by this filter.}

\item{verbose}{\verb{[integer]}\cr Level of console output (default \code{1}).\cr
\code{0} = silent, \code{1} = one-line summary, \code{2} = detailed per-shape breakdown.}

\item{dry_run}{\verb{[logical]}\cr If \code{TRUE}, no replacements are performed. The log is still
collected and attached to the returned object (default \code{FALSE}).}
}
\value{
The (modified) rpptx object with an attribute \code{"text_replace_log"} containing a
tibble of replacements performed. Use \code{\link[=text_replace_log]{text_replace_log()}} to access it.
}
\description{
All occurrences of \code{pattern} on the selected slides are replaced with the text of \code{replacement}.
For the formatting, only the first character of the matched pattern is relevant.
The whole replacement will have this format.

\code{text_replace()} was inspired by the Python project
\href{https://github.com/fschaeck/python-pptx-text-replacer}{python-pptx-text-replacer}.
}
\examples{
file_in <- example_pptx("text_replace")
file_out <- tempfile(fileext = ".pptx")

# file_open(file_in)

x <- read_pptx(file_in)
x <- text_replace(x, "{1}" = ">>>", "@" = ":)")
print(x, target = file_out)

# file_open(file_out)
}
