% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shape-select.R
\name{shape_select}
\alias{shape_select}
\title{Select top-level shapes on slides}
\usage{
shape_select(
  x,
  slide_idx = NULL,
  text = NULL,
  name = NULL,
  id = NULL,
  kind = NULL,
  placeholder = NULL,
  ph_type = NULL,
  hidden = NULL,
  match = c("contains", "exact", "regex")
)
}
\arguments{
\item{x}{\verb{[rpptx]}\cr An \code{rpptx} object returned by \code{\link[officer:read_pptx]{officer::read_pptx()}}.}

\item{slide_idx}{\verb{[integer]}\cr Slide indexes to search. If \code{NULL} (default),
all slides are searched.}

\item{text}{\verb{[character]}\cr Visible text to match.}

\item{name}{\verb{[character]}\cr Shape names as shown in PowerPoint's Selection Pane.}

\item{id}{\verb{[character]}\cr Current XML shape IDs. These are slide-local and not
stable across save operations.}

\item{kind}{\verb{[character]}\cr Shape kinds to include, e.g. \code{"shape"},
\code{"picture"}, \code{"table"}, \code{"chart"}, \code{"graphic_frame"}, \code{"group"},
or \code{"connector"}.}

\item{placeholder}{\verb{[logical]}\cr If supplied, filter by whether the object is
an actual PowerPoint placeholder.}

\item{ph_type}{\verb{[character]}\cr Placeholder types to include.}

\item{hidden}{\verb{[logical]}\cr If supplied, filter by the shape's hidden flag.}

\item{match}{\verb{[character(1)]}\cr Matching mode for \code{text} and \code{name}.
\code{"contains"} (default) uses fixed substring matching. \code{"exact"} compares
complete values after trimming leading and trailing whitespace. \code{"regex"}
uses regular expressions.}
}
\value{
A tibble-like \code{pptx_shape_selection} object with one row per selected
top-level shape. The \code{node} list-column contains the underlying XML node.
}
\description{
\code{shape_select()} returns metadata and XML node references for existing
top-level objects on one or more slides. It is intended for workflows where
manually placed shapes serve as markers for later processing.
}
\details{
This function only selects shapes. It does not insert, replace, hide, or
remove slide content.
}
\examples{
x <- read_pptx()
x <- add_slide(x, layout = "Blank")
x <- ph_with(
  x,
  "{image_1}",
  location = ph_location(
    left = 1, top = 1, width = 2, height = 1,
    newlabel = "Image marker"
  )
)

# Select every top-level shape whose visible text contains the marker.
shape_select(x, text = "{image_1}")

# Use exact matching when the full marker text should match. Leading and
# trailing whitespace in the shape text is ignored.
shape_select(x, text = "{image_1}", match = "exact")
}
